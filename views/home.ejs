<html>
  <!-- I see you prefer to appreciate websites in their raw text form. 
    You might enjoy heading over to emmanuelsuarez.com/rawtext.html -->
  <head>
      <title><%= title %></title>
      <link rel="stylesheet" href="stylesheets/style.css" />
      <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.0/css/all.css" integrity="sha384-Mmxa0mLqhmOeaE8vgOSbKacftZcsNYDjQzuCOm6D02luYSzBG8vpaOykv9lFQ51Y" crossorigin="anonymous">
      <link rel="shortcut icon" type="image/x-icon" href="favicon.ico"/>
  </head>

  <body>
      <div class="center-text">
        <h1 class=<%= isMobile ? "mobile-h1" : "desktop-h1" %>>Emmanuel Su&aacute;rez</h1>
        <div>
          Born and raised in Puerto Rico
          <% if (!isMobile) { %>
            <img class="puerto-rico" src="images/puerto-rico.png">
          <% } %>
        </div>
        <h4>Senior at the University of Pennsylvania</h4>
        <h4>Incoming Software Engineer at Strava</h4>
      </div>
      <div class="center-text">
          <a class="inline-block" href="https://www.linkedin.com/in/emsuac">
            <% if (!isMobile) { %>
              <i class="fab fa-linkedin fa-2x"></i>
            <% } else { %>
              <i class="fab fa-linkedin fa-4x"></i>
            <% } %>
          </a>
          <a class="inline-block" href="https://www.github.com/emmanueljs1">
            <% if (!isMobile) { %>
              <i class="fab fa-github fa-2x"></i>
            <% } else { %>
              <i class="fab fa-github fa-4x"></i>
            <% } %>
          </a>
          <a class="inline-block" href="mailto:emmanueljs1@gmail.com">
            <% if (!isMobile) { %>
              <i class="fas fa-envelope fa-2x"></i>
            <% } else { %>
              <i class="fas fa-envelope fa-4x"></i>
            <% } %>
          </a>
          <a class="inline-block" href="files/resume.pdf">
            <% if (!isMobile) { %>
              <i class="fas fa-file-alt fa-2x"></i>
            <% } else { %>
              <i class="fas fa-file-alt fa-4x"></i>
            <% } %>
          </a>
      </div>
      <% if (!isMobile) { %>
        <div class="house-container">
          <div class="house"></div>
          <h4 class><a href="/">Click To Play</a></h4>
        </div>
      <% } %>
  </body>
</html>
